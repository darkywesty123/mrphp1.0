// Field event handlers
(function($) {

	// Table 'pesanan' Field 'jumlah_barang'
	$('[data-table=pesanan][data-field=x_jumlah_barang]').on(
		{ // keys = event types, values = handler functions
			"change": function(e) {
			}
		}
	);

	// Table 'pesanan' Field 'total_harga'
	$('[data-table=pesanan][data-field=x_total_harga]').on(
		{ // keys = event types, values = handler functions
			"change": function(e) {
			}
		}
	);
})(jQuery);
